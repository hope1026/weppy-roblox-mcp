# WEPPY Roblox AI Toolkit

> WEPPY Roblox AI Toolkit is a Roblox Studio AI development product. It includes
> the WEPPY MCP Server for AI app connections and the WEPPY Roblox Studio Plugin
> for live Studio control. Claude Code, Codex, and Antigravity can also install
> the optional WEPPY AI Agent Plugin.

## What it does

The WEPPY MCP Server and WEPPY Roblox Studio Plugin provide a real-time bridge
between AI agents and the Roblox Studio DataModel. The server exposes the full Roblox API surface as executable MCP tools,
so AI can directly create, read, modify, and delete instances, scripts, properties,
terrain, and more — inside a live Studio session.

- Action-based MCP tools covering the full Roblox Studio API surface
- Basic Luau syntax validation for raw source or an existing Script without starting Playtest
- Bidirectional project sync between Roblox Studio and local files (Pro)
- Structured Playtest: ordinary checks use Play and record UI, input, and server evidence locally
- UI Studio: AI creates in-game UI or analyzes existing Edit and Play Client UI; an unreviewed runtime is reported as missing evidence, and Play semantic checks remain separate from unsupported Play screenshots
- Web dashboard: monitor AI actions, tool history, sync state, changelog, and multi-agent / multi-Studio Connection topology with Studio Targets at `http://localhost:3002`
- Multi-place support (up to 5 places simultaneously)
- Roblox Explorer VSCode extension for browsing Studio hierarchy
- Works with Claude Code, Claude Desktop, Cursor, Codex CLI, Codex App, Gemini CLI, Antigravity / Antigravity IDE / Antigravity CLI

## Sync policy and safety

Sync starts Off on first use. Start on connection is also Off. Saved settings always take precedence, including explicitly saved Off and false values. The default first-use workflow is Studio First, with Studio as the source for the initial synchronization.

The plugin offers Studio First, Bidirectional Review, Local Code, Studio World, and Custom workflows. Custom uses four semantic scopes: Script Source, Properties, Structure, and Service Properties. Content Changes and Structure Changes application modes are separate from those scope directions. Local file deletion does not delete a Studio instance by default; applying it to Studio requires an additional opt-in and is separate from UI Studio history cleanup. The Dashboard reports the effective policy read-only, while the Studio plugin owns Sync start, stop, and policy editing.

The WEPPY MCP Server and WEPPY Roblox Studio Plugin must come from the same release. Update both together, then restart Roblox Studio before reconnecting if their versions do not match.

## Luau syntax validation

`manage_scripts.validate` checks either raw source or an existing Script path and
returns syntax diagnostics without starting Playtest. It is available on Basic
and Pro. It checks only whether the Luau source parses.

The Dashboard setting at **Controls → Luau Syntax Validation** enforces whether
successful `set_source`, `edit_replace`, `edit_insert`, and `edit_delete` calls
validate the actual saved source. It defaults to Off. A conflicting tool
`validate` value is ignored and a warning directs the user to that Dashboard
setting. Validation never rolls back an applied write.

Automatic valid checks do not expand the normal write response. Invalid or
unavailable checks add details; an explicit matching `validate=true` requests
full valid details too. The first request that needs WASM downloads the exact
`luau-parser-v1.0.0` companion release and caches it locally. A compatible local
`luau-compile` is the fallback. Source is parsed on the user's computer and is
not sent to a remote validation service. If no parser is available, the action
reports validation as unavailable without turning a successful write into a
failure.

## Playtest verification

Ordinary runtime verification uses a structured Play session. The AI can wait
for UI, inspect semantic structure and geometry, deliver virtual input, observe
activation separately, and verify the server result in another step. The local
Dashboard groups saved evidence by server, client UI, interaction, and visual
status.

The Playtest Dashboard also has a Test Values workspace. Users can save multiple
profiles per Place, and the Dashboard restores the last profile and the last
selected Test Values or Automated Test Results workspace. Its shared session bar keeps
the Studio state visible without starting or stopping Play. Users start Play in
Studio Editor, and the Dashboard attaches the selected profile to that active
session. They can switch, create, edit, or delete profiles while Play or Run
continues; switching keeps current game values and replaces the controls, Watch
Values, and Actions shown for the same session. Profile editing can add controls and read-only Watch Values from a
runtime path or current Studio selection. Controls support number, min/max
range, string, and boolean checkbox inputs. Basic includes profile creation and Watch Values. Pro adds typed value
changes, rollback, Reset all across reversible changes made through every profile in the session, and actual saved Automated Test Results. Watch Values shows
fresh server or client state and moves disconnected or
out-of-sequence data to a separate last-observed state. Project Test Adapters
are reviewed development code with bounded calls and required cleanup.

Before publishing is available from the Playtest page header on Basic and Pro as a plain-language, read-only
reference. It explains what returns to its original value after a Studio test,
what WEPPY keeps only on the user's computer, and what to check before and after
publishing. Common checks are separated from items for games with test-only
features or saved player data. Test connection code created with default
settings does not run in the public game. If a local WEPPY record shows that
Studio-only protection was explicitly removed, the Dashboard adds a note only
for that Place. Rojo remains optional for omitting test source from a production
project. Currency or progress saved by game code can remain after a test, and
Places in the same Experience can share that data. WEPPY does not scan or
certify deployment files, remove test code, build deployment copies, or publish
through this guide.

Run mode is for an explicitly requested server-only check. Raw Luau is an
advanced explicit opt-in when a diagnostic does not fit the structured steps.
Play-mode screenshot capture is not supported; semantic UI, input delivery,
activation, and server observations are recorded instead. Existing Raw Luau
reports remain readable and no setting change is required.

## Installation

**Install page** (recommended starting point):

https://weppyai.com/en/install

The install page shows the recommended one-line script first.

```bash
# macOS / Linux
curl -fsSL https://raw.githubusercontent.com/hope1026/weppy-roblox-mcp/main/install.sh | bash

# Windows (PowerShell)
irm https://raw.githubusercontent.com/hope1026/weppy-roblox-mcp/main/install.ps1 | iex
```

Use the terminal-free web installer on the same page if terminal or PowerShell is uncomfortable.

If automated install fails, use the app-specific manual guides from the docs.

## Optional WEPPY AI Agent Plugin

Claude Code, Codex, and Antigravity can additionally install the WEPPY AI Agent Plugin (`weppy-roblox-ai-toolkit`) for client-native setup and workflow guidance. Other supported AI apps connect through the WEPPY MCP Server.

Claude Code:

```bash
claude plugin marketplace add hope1026/weppy-roblox-mcp --scope user
claude plugin install weppy-roblox-ai-toolkit@hope1026-roblox-mcp --scope user
```

Codex:

```bash
codex plugin marketplace add hope1026/weppy-roblox-mcp
```

After adding the Codex marketplace, restart Codex, open Plugin Directory, and install WEPPY AI Agent Plugin.

Antigravity:

- Antigravity CLI is required to install the WEPPY AI Agent Plugin. The installer uses `agy plugin install` with the latest public GitHub release and verifies it with `agy plugin list`.
- Existing plugins are replaced on every run. Results are installed, updated, reinstalled, repaired, fallback, failed, or skipped; an existing plugin is never treated as a skip.
- The shared MCP fallback is `~/.gemini/config/mcp_config.json`. In a CLI-only profile, verified native plugin MCP replaces the shared entry. When Antigravity IDE and CLI coexist, both plugin views stay skill-only and one shared direct `weppy-roblox-mcp` definition serves both.
- Outside the verified OS and version matrix, native discovery uses the MCP fallback. Other MCP servers are preserved.
- On Windows Antigravity, CLI installation has been verified to expose skills and MCP. Windows Antigravity IDE plugin discovery is unverified because the verified environment has no plugin or skills inspection UI, so the shared MCP fallback is preserved.
- Do not pass a GitHub URL directly to `agy plugin install`; the installer downloads the latest public release and installs from a prepared local path.

## Tiers

- Basic (Free): MCP tool execution + Studio-to-local sync
- Pro: Additional Sync workflows, UI Studio, bulk actions, terrain generation, spatial analysis, multi-place support for up to 5 places, advanced tools

## Detailed docs

- Install and setup: https://weppyai.com/en/install
- Pro upgrade: https://weppyai.com/plans/

## Requirements

- Node.js >= 18
- Roblox Studio (Windows 10+ or macOS 12+)
- Any MCP-compatible AI client

## Optional

- Roblox Explorer VSCode extension (browse synced instance tree)
- Pro subscription for bidirectional sync, UI Studio, and advanced tools

## Links

- GitHub: https://github.com/hope1026/weppy-roblox-mcp
- npm: https://www.npmjs.com/package/@weppy/roblox-mcp
- Roblox Explorer (VS Code Marketplace): https://marketplace.visualstudio.com/items?itemName=weppy.weppy-roblox-explorer
- Roblox Explorer (Open VSX): https://open-vsx.org/extension/weppy/weppy-roblox-explorer
- Issues: https://github.com/hope1026/weppy-roblox-mcp/issues
